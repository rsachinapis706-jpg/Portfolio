import React from "react";
import ReactDOM from "react-dom/client";
import SachinPortfolio from "./portfolio";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SachinPortfolio />
  </React.StrictMode>
);
